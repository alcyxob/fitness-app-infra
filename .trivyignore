# Trivy ignore file - known issues tracked in backlog
# These will be addressed in future iterations

# App Runner: secrets passed as plain-text environment variables
# Backlog: migrate to AWS Secrets Manager / SSM Parameter Store references
AVD-AWS-0057

# S3: no bucket policy attached
# Backlog: add explicit deny policy for defense-in-depth (public access already blocked)
AVD-AWS-0088

# ECR: image tags are mutable
# Backlog: set to IMMUTABLE for production, acceptable for dev with 'latest' tag
AVD-AWS-0031

# SNS: topic encryption uses AWS-managed key instead of CMK
# Acceptable for dev environment; CMK adds cost with minimal security benefit for alerts
AVD-AWS-0136

# S3: Trivy flags bucket_key_enabled pattern even with KMS CMK configured
AVD-AWS-0132
